import express from 'express';
import type { Request, Response } from 'express';
import { Service } from '../../models/index.js';

const router = express.Router();

// GET /service - Get all services
router.get('/', async (_req: Request, res: Response) => {
  try {
    const services = await Service.findAll();
    res.status(200).json(services);
  } catch (error) {
    res.status(500).json({ error: 'Internal Server Error' });
  }
});

// GET /service/:id
router.get('/:id', async (req: Request, res: Response) => {
  try {
    const service = await Service.findByPk(req.params.id);
    if (service) {
      res.status(200).json(service);
    } else {
      res.status(404).json({ error: 'Service not found' });
    }
  } catch (error) {
    res.status(500).json({ error: 'Internal Server Error' });
  }
});

// POST /service - Create new service
router.post('/api/service', async (req: Request, res: Response) => {
  try {
    const newService = await Service.create(req.body);
    res.status(201).json(newService);
  } catch (error) {
    res.status(500).json({ error: 'Internal Server Error' });
  }
});

// PUT /service/:id - Update service by ID
router.put('/:id', async (req: Request, res: Response) => {
  try {
    //We need to find the service with the id from the params object
    const service = await Service.findByPk(req.params.id);
    if (service) {
      //We then need to update that service with what is sent over in the request body
      await service.update(req.body);
      res.status(200).json(service);
    } else {
      res.status(404).json({ error: 'Service not found' });
    }
  } catch (error) {
    res.status(500).json({ error: 'Internal Server Error' });
  }
});

// DELETE /service/:id - Delete service by ID
router.delete('/:id', async (req: Request, res: Response) => {
  try {
    const service = await Service.findByPk(req.params.id);
    if (service) {
      //We use the sequelize method destroy to delete the service from the database
      await service.destroy();
      res.status(204).send();
    } else {
      res.status(404).json({ error: 'Service not found' });
    }
  } catch (error) {
    res.status(500).json({ error: 'Internal Server Error' });
  }
});

export { router as serviceRouter };